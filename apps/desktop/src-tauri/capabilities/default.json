{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the bundled connection screen may do. Deliberately small: it needs to read and write one setting, probe an address, and navigate. Nothing here reaches the filesystem or the shell.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
